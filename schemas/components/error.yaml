# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
additionalProperties: false
description: Operation failure reply. A device error code is only attached to DeviceCommandError,
  and only when the controller reports one.
properties:
  status:
    const: error
    default: error
    type: string
  error_type:
    $ref: ./error_type.yaml
    description: Concrete error type to raise on the client.
  message:
    description: Human-readable failure message.
    type: string
  code:
    anyOf:
    - type: integer
    - type: 'null'
    default: null
    description: Device error code, when reported by the controller.
required:
- error_type
- message
title: Error
type: object
