# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
additionalProperties: false
description: 'TRIGGER-mode parameter pair for one channel. Stored independently of
  the trigger profile: this pair caps the profile''s step currents and selects the
  trigger edge, while the profile holds the steps themselves. The device silently
  clamps an over-ceiling limit instead of rejecting it, so an acknowledged write is
  not proof of what was stored — read back to verify.'
properties:
  current_max_ma:
    description: TRIGGER-mode current limit, in mA
    minimum: 0
    type: number
  polarity:
    $ref: ./trigger_polarity.yaml
    description: Trigger input edge that starts playback
required:
- current_max_ma
- polarity
title: TriggerParameters
type: object
