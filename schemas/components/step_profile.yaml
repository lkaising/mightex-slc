# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
additionalProperties: false
description: 'An ordinary profile: a sequence of timed steps, played in order. An
  empty profile is valid and means the channel does nothing on trigger — the device''s
  own cleared state, and the way to clear a stored profile. The wire-level (0, 0)
  terminator step is not part of the model; the codec appends it. How many steps a
  given controller can store is a module capability, enforced where capabilities are
  known.'
properties:
  kind:
    const: steps
    default: steps
    type: string
  steps:
    description: Timed steps, played in wire order
    items:
      $ref: ./profile_step.yaml
    maxItems: 127
    type: array
required:
- steps
title: StepProfile
type: object
