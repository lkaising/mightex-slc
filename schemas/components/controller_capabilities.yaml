# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
additionalProperties: false
description: Read-only limits and feature flags for an opened controller. Returned
  by the server so clients can choose valid operations without duplicating vendor
  module tables.
properties:
  module_type:
    $ref: ./module_type.yaml
    description: Controller module family.
  channel_count:
    description: Number of LED output channels.
    minimum: 1
    type: integer
  current_resolution_ma:
    description: Smallest settable current increment, in milliamps.
    exclusiveMinimum: 0
    type: number
  max_profile_steps:
    description: Maximum programmable current/time pairs before the required (0, 0)
      terminator.
    maximum: 127
    minimum: 2
    type: integer
  supports_trigger_mode:
    description: Whether TRIGGER mode is available.
    type: boolean
  supports_load_voltage:
    description: Whether load-voltage read-back is available.
    type: boolean
  supports_fan_control:
    description: Whether FanPWM control is available.
    type: boolean
required:
- module_type
- channel_count
- current_resolution_ma
- max_profile_steps
- supports_trigger_mode
- supports_load_voltage
- supports_fan_control
title: ControllerCapabilities
type: object
