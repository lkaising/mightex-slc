# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
additionalProperties: false
description: Identity data for a discovered controller. The SDK discovers controllers
  by count and opens them by zero-based index. Serial number, module family, and channel
  count require an open device handle, so they may be None in discovery-only responses.
properties:
  index:
    description: Discovery-session index for open_device
    minimum: 0
    type: integer
  serial_number:
    anyOf:
    - type: string
    - type: 'null'
    default: null
    description: Controller serial number, when resolved.
  module_type:
    anyOf:
    - $ref: ./module_type.yaml
    - type: 'null'
    default: null
    description: Controller module family, when resolved.
  channel_count:
    anyOf:
    - minimum: 1
      type: integer
    - type: 'null'
    default: null
    description: Number of output channels, when resolved.
required:
- index
title: DeviceDescriptor
type: object
