# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
additionalProperties: false
description: Identity of a connected controller as seen during discovery. Only the
  number of connected controllers is knowable before a controller is opened. Serial
  number, module type, and channel count are read through functions that need an open
  device, so they are None here until the device is opened and are never guessed.
properties:
  index:
    description: Zero-based device index for open_device
    minimum: 0
    type: integer
  serial_number:
    anyOf:
    - type: string
    - type: 'null'
    default: null
    description: Serial number if readable before opening, else None
  module_type:
    anyOf:
    - $ref: ./module_type.yaml
    - type: 'null'
    default: null
    description: Module family if readable before opening, else None
  channel_count:
    anyOf:
    - minimum: 1
      type: integer
    - type: 'null'
    default: null
    description: Channel count if readable before opening, else None
required:
- index
title: DeviceDescriptor
type: object
