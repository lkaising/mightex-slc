# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
additionalProperties: false
description: 'A trigger-only profile: the output follows the trigger input level.
  While the trigger input is asserted the channel drives current_ma; otherwise it
  is off. This is the explicit spelling of the device''s reserved first-step duration,
  which never appears in a public field.'
properties:
  kind:
    const: follower
    default: follower
    type: string
  current_ma:
    description: Drive current while the trigger input is asserted, in mA
    minimum: 0
    type: number
required:
- current_ma
title: FollowerProfile
type: object
