# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
operation: set_active_mode
request:
  additionalProperties: false
  description: Switch a channel to an active working mode.
  properties:
    device_id:
      description: Handle returned by open_device
      type: string
    channel:
      description: One-based channel number
      minimum: 1
      type: integer
    mode:
      $ref: ../components/operating_mode.yaml
      description: Working mode to select
  required:
  - device_id
  - channel
  - mode
  title: SetActiveModeRequest
  type: object
reply:
  $defs:
    SetActiveModeOk:
      additionalProperties: false
      description: Active mode changed successfully.
      properties:
        status:
          const: ok
          default: ok
          type: string
      title: SetActiveModeOk
      type: object
  discriminator:
    mapping:
      error: ../components/error.yaml
      ok: '#/$defs/SetActiveModeOk'
    propertyName: status
  oneOf:
  - $ref: '#/$defs/SetActiveModeOk'
  - $ref: ../components/error.yaml
