# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
operation: get_active_mode
request:
  additionalProperties: false
  description: Read back the mode currently driving a channel.
  properties:
    device_id:
      description: Handle returned by open_device
      type: string
    channel:
      description: One-based channel number
      minimum: 1
      type: integer
  required:
  - device_id
  - channel
  title: GetActiveModeRequest
  type: object
reply:
  $defs:
    GetActiveModeOk:
      additionalProperties: false
      description: Successful get-active-mode reply.
      properties:
        status:
          const: ok
          default: ok
          type: string
        result:
          $ref: ../components/operating_mode.yaml
          description: The mode currently driving the channel
      required:
      - result
      title: GetActiveModeOk
      type: object
  discriminator:
    mapping:
      error: ../components/error.yaml
      ok: '#/$defs/GetActiveModeOk'
    propertyName: status
  oneOf:
  - $ref: '#/$defs/GetActiveModeOk'
  - $ref: ../components/error.yaml
