# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
operation: configure_normal
request:
  additionalProperties: false
  description: 'Store NORMAL mode parameters for a channel. Runtime-only rule (does
    not export to JSON Schema): current_set_ma must not exceed current_max_ma.'
  properties:
    device_id:
      description: Handle returned by open_device
      type: string
    channel:
      description: One-based channel number
      minimum: 1
      type: integer
    current_max_ma:
      description: Maximum current in milliamps
      minimum: 0
      type: number
    current_set_ma:
      description: Working current in milliamps
      minimum: 0
      type: number
  required:
  - device_id
  - channel
  - current_max_ma
  - current_set_ma
  title: ConfigureNormalRequest
  type: object
reply:
  $defs:
    ConfigureNormalOk:
      additionalProperties: false
      description: Successful configure-normal reply.
      properties:
        status:
          const: ok
          default: ok
          type: string
      title: ConfigureNormalOk
      type: object
  discriminator:
    mapping:
      error: ../components/error.yaml
      ok: '#/$defs/ConfigureNormalOk'
    propertyName: status
  oneOf:
  - $ref: '#/$defs/ConfigureNormalOk'
  - $ref: ../components/error.yaml
