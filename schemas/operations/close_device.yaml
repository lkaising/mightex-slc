# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
operation: close_device
request:
  additionalProperties: false
  description: Close the controller handle and release it.
  properties:
    device_id:
      description: Handle returned by open_device
      type: string
  required:
  - device_id
  title: CloseDeviceRequest
  type: object
reply:
  $defs:
    CloseDeviceOk:
      additionalProperties: false
      description: Successful close reply.
      properties:
        status:
          const: ok
          default: ok
          type: string
      title: CloseDeviceOk
      type: object
  discriminator:
    mapping:
      error: ../components/error.yaml
      ok: '#/$defs/CloseDeviceOk'
    propertyName: status
  oneOf:
  - $ref: '#/$defs/CloseDeviceOk'
  - $ref: ../components/error.yaml
