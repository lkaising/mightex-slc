# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
operation: enumerate_devices
request:
  additionalProperties: false
  description: Request to scan for connected controllers. Carries no fields.
  properties: {}
  title: EnumerateDevicesRequest
  type: object
reply:
  $defs:
    EnumerateDevicesOk:
      additionalProperties: false
      description: Successful discovery reply.
      properties:
        status:
          const: ok
          default: ok
          type: string
        result:
          description: One descriptor per connected controller, in index order
          items:
            $ref: ../components/device_descriptor.yaml
          type: array
      required:
      - result
      title: EnumerateDevicesOk
      type: object
  discriminator:
    mapping:
      error: ../components/error.yaml
      ok: '#/$defs/EnumerateDevicesOk'
    propertyName: status
  oneOf:
  - $ref: '#/$defs/EnumerateDevicesOk'
  - $ref: ../components/error.yaml
