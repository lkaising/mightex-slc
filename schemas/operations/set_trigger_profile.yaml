# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
operation: set_trigger_profile
request:
  additionalProperties: false
  description: Store a trigger profile for a channel.
  properties:
    device_id:
      description: Handle returned by open_device
      type: string
    channel:
      description: One-based channel number
      minimum: 1
      type: integer
    profile:
      description: Trigger profile to store
      discriminator:
        mapping:
          follower: ../components/follower_profile.yaml
          steps: ../components/step_profile.yaml
        propertyName: kind
      oneOf:
      - $ref: ../components/step_profile.yaml
      - $ref: ../components/follower_profile.yaml
  required:
  - device_id
  - channel
  - profile
  title: SetTriggerProfileRequest
  type: object
reply:
  $defs:
    SetTriggerProfileOk:
      additionalProperties: false
      description: Set-trigger-profile succeeded.
      properties:
        status:
          const: ok
          default: ok
          type: string
      title: SetTriggerProfileOk
      type: object
  discriminator:
    mapping:
      error: ../components/error.yaml
      ok: '#/$defs/SetTriggerProfileOk'
    propertyName: status
  oneOf:
  - $ref: '#/$defs/SetTriggerProfileOk'
  - $ref: ../components/error.yaml
