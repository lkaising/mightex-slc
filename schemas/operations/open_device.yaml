# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
operation: open_device
request:
  additionalProperties: false
  description: Open the controller at a serial port.
  properties:
    port:
      anyOf:
      - minLength: 1
        type: string
      - type: 'null'
      default: null
      description: Serial port device path of the controller to open (e.g. /dev/cu.usbserial-A6002xyz);
        None uses the backend's configured default target
  title: OpenDeviceRequest
  type: object
reply:
  $defs:
    OpenDeviceOk:
      additionalProperties: false
      description: Controller opened successfully.
      properties:
        status:
          const: ok
          default: ok
          type: string
        device_id:
          description: Opaque device id for subsequent operations
          type: string
        serial_number:
          description: Serial number of the opened controller
          type: string
        capabilities:
          $ref: ../components/controller_capabilities.yaml
          description: Capabilities of the opened controller
      required:
      - device_id
      - serial_number
      - capabilities
      title: OpenDeviceOk
      type: object
  discriminator:
    mapping:
      error: ../components/error.yaml
      ok: '#/$defs/OpenDeviceOk'
    propertyName: status
  oneOf:
  - $ref: '#/$defs/OpenDeviceOk'
  - $ref: ../components/error.yaml
