# Generated by scripts/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in src/mightex_slc/contract/.
operation: restore_factory_defaults
request:
  additionalProperties: false
  description: Load factory defaults into the current settings of all channels and
    modes. This changes the current (volatile) settings only; persist_settings writes
    them to non-volatile memory.
  properties:
    device_id:
      description: Handle returned by open_device
      type: string
  required:
  - device_id
  title: RestoreFactoryDefaultsRequest
  type: object
reply:
  $defs:
    RestoreFactoryDefaultsOk:
      additionalProperties: false
      description: Factory defaults restored successfully.
      properties:
        status:
          const: ok
          default: ok
          type: string
      title: RestoreFactoryDefaultsOk
      type: object
  discriminator:
    mapping:
      error: ../components/error.yaml
      ok: '#/$defs/RestoreFactoryDefaultsOk'
    propertyName: status
  oneOf:
  - $ref: '#/$defs/RestoreFactoryDefaultsOk'
  - $ref: ../components/error.yaml
