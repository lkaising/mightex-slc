# Phase Status — `normal_mode_timed_on` slice

Progress ledger for [`build_plan_normal_mode_timed_on.md`](build_plan_normal_mode_timed_on.md).
One row per phase; the plan holds the detail, this file holds only where we
are. Update the row when a phase's gate passes.

Statuses: `not started` / `in progress` / `complete`.

| Phase | Scope | Gate / completion condition | Status | Completion note |
|---|---|---|---|---|
| 0 | Prep: `pyproject.toml`, root `__init__.py` stub, this ledger | `python -m pip install -e ".[dev]"` succeeds; `import mightex_slc` works | complete | 2026-07-04. Hatchling + src/ layout authored (Python >=3.11); root stub created; build plan clarified (Phase 0/1 split; `NormalParameters` question recorded as Phase 1 investigation). Gate passed: editable install from the sibling `examples/` environment succeeded; `import mightex_slc` resolved to `src/mightex_slc/__init__.py`; metadata reported version 0.0.0. |
| 1 | Contract foundation: port six operations, six components, two bases; write schema generator; generate schemas | `import mightex_slc.contract` works; each operation's valid request round-trips, invalid rejected; schemas generated | complete | 2026-07-04. Contract models, schema generator, and generated schemas implemented; smoke verification passed (`import mightex_slc.contract` works, all six valid requests round-trip via `model_dump(mode="json")`, a reply union validates both branches, and invalid requests reject: set > max, negative current, channel < 1, missing `device_id`). The generator emits each shared shape once in its component file and cross-references it, with a consistency check on every run; keeping `schemas/` current means rerunning the generator after any contract-model change. Open investigation unchanged: `NormalParameters` read-path-only vs. reuse in `configure_normal` (plan §6, Phase 1); `normal_parameters.py` stays a dormant stub. |
| 2 | Transport seam and fake: `transport/base` interface, in-memory fake device | Fake implements the interface with per-channel state and real configure-then-activate semantics | complete | 2026-07-06. `transport/base.py` defines the seam: a `Transport` ABC carrying the slice's six operations, the opaque `TransportHandle` (not the public `device_id` — that mapping is the Phase 4 session's), `TransportOpenResult` (handle, serial number, capabilities), and the exception family rooted at `TransportError`. `transport/fake` implements it as one in-memory SLC-MA04-MU (4 channels, 1 mA resolution, trigger absent, fan control present, `requires_initialization=True` — every capability a documented fact for that variant) with per-channel state at the documented factory defaults (DISABLE, NORMAL 20/10 mA). Configure stores without changing output; only `set_active_mode` changes it; channel state persists across an idempotent, identity-checked close. Gate passed via a throwaway smoke script: enumerate → open (capabilities verified) → initialize → configure (mode stays DISABLE, params stored) → NORMAL → DISABLE → close, plus bad-index, busy-open, out-of-range channel/current, TRIGGER-on-MA, and stale-handle rejections; `ruff check src/` clean. rs232 files remain docstring-only. |
| 3 | Tracer bullet: `enumerate_devices` end to end | A throwaway-script run returns a non-empty descriptor list from the fake through the full seam | not started | |
| 4 | Fan out the remaining five operations | Each of the six operations round-trips against the fake in isolation | not started | |
| 5 | Public surface: errors, types, discovery, proxies, root `__init__.py` exports | The example's imports resolve from `mightex_slc` | not started | |
| 6 | Acceptance: run the example | `examples/normal_mode_timed_on.py` completes cleanly against the fake, with the expected fake end state; one hand-checked error path (bad open index raises `DeviceNotFoundError`) | not started | |
