# Generated by contract/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in contract/mightex_contract/.
ProfileStep:
  additionalProperties: false
  description: 'A single profile step. Note: the public API skeleton expresses a step
    as a bare tuple (current_ma, time_us). The contract deliberately replaces that
    tuple with this named model, so the serialized form is {"current_ma": 10.0, "time_us":
    1000} rather than [10.0, 1000]. That object form is the intended public surface.'
  properties:
    current_ma:
      description: Step current in milliamps
      minimum: 0
      title: Current Ma
      type: number
    time_us:
      description: Step duration in whole microseconds
      minimum: 0
      title: Time Us
      type: integer
  required:
  - current_ma
  - time_us
  title: ProfileStep
  type: object
Profile:
  $defs:
    ProfileStep:
      additionalProperties: false
      description: 'A single profile step. Note: the public API skeleton expresses
        a step as a bare tuple (current_ma, time_us). The contract deliberately replaces
        that tuple with this named model, so the serialized form is {"current_ma":
        10.0, "time_us": 1000} rather than [10.0, 1000]. That object form is the intended
        public surface.'
      properties:
        current_ma:
          description: Step current in milliamps
          minimum: 0
          title: Current Ma
          type: number
        time_us:
          description: Step duration in whole microseconds
          minimum: 0
          title: Time Us
          type: integer
      required:
      - current_ma
      - time_us
      title: ProfileStep
      type: object
  items:
    $ref: '#/$defs/ProfileStep'
  maxItems: 127
  type: array
