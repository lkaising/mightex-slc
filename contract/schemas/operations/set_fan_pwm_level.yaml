# Generated by contract/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in contract/mightex_contract/.
operation: set_fan_pwm_level
request:
  additionalProperties: false
  description: Set the cooling fan drive level.
  properties:
    device_id:
      description: Handle returned by open_device
      title: Device Id
      type: string
    level:
      description: Fan drive level, 0 fully off to 10 fully on, in 10 percent steps
      maximum: 10
      minimum: 0
      title: Level
      type: integer
  required:
  - device_id
  - level
  title: SetFanPwmLevelRequest
  type: object
reply:
  $defs:
    Error:
      additionalProperties: false
      description: 'Error reply envelope shared by every operation. Runtime-only rule
        (does not export to JSON Schema): code is populated only for DeviceCommandError.
        It may still be None for a DeviceCommandError when the device reports no code.'
      properties:
        status:
          const: error
          default: error
          title: Status
          type: string
        error_type:
          $ref: '#/$defs/ErrorType'
          description: Library exception name to raise
        message:
          description: Human-readable error description
          title: Message
          type: string
        code:
          anyOf:
          - type: integer
          - type: 'null'
          default: null
          description: Device-reported error code, present only for DeviceCommandError
          title: Code
      required:
      - error_type
      - message
      title: Error
      type: object
    ErrorType:
      description: Library exception names reported in an error reply. These are the
        leaf exception types the client maps an error reply back onto. The abstract
        root MightexLEDError is intentionally absent because it is never raised directly.
        DEVICE_CONNECTION and DEVICE_NOT_FOUND are both present even though the latter
        is a subtype of the former, so the client can map directly to the most specific
        class.
      enum:
      - ValueError
      - ControllerClosedError
      - DeviceConnectionError
      - DeviceNotFoundError
      - DeviceCommandError
      - UnsupportedOperationError
      title: ErrorType
      type: string
    SetFanPwmLevelOk:
      additionalProperties: false
      description: Successful set-fan-pwm-level reply.
      properties:
        status:
          const: ok
          default: ok
          title: Status
          type: string
      title: SetFanPwmLevelOk
      type: object
  discriminator:
    mapping:
      error: '#/$defs/Error'
      ok: '#/$defs/SetFanPwmLevelOk'
    propertyName: status
  oneOf:
  - $ref: '#/$defs/SetFanPwmLevelOk'
  - $ref: '#/$defs/Error'
