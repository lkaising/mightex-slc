# Generated by contract/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in contract/mightex_contract/.
operation: device_info
request:
  additionalProperties: false
  description: Request identifying information from an open controller.
  properties:
    device_id:
      description: Handle returned by open_device
      type: string
  required:
  - device_id
  title: DeviceInfoRequest
  type: object
reply:
  $defs:
    DeviceInfo:
      additionalProperties: false
      description: Identifying information reported by a controller.
      properties:
        device_type:
          description: Module or device type string
          type: string
        firmware_version:
          description: Firmware version string
          type: string
        serial_number:
          description: Serial number string
          type: string
        raw:
          description: Full information line exactly as reported
          type: string
      required:
      - device_type
      - firmware_version
      - serial_number
      - raw
      title: DeviceInfo
      type: object
    DeviceInfoOk:
      additionalProperties: false
      description: Successful device-info reply.
      properties:
        status:
          const: ok
          default: ok
          type: string
        result:
          $ref: '#/$defs/DeviceInfo'
          description: Identifying information reported by the controller
      required:
      - result
      title: DeviceInfoOk
      type: object
    Error:
      additionalProperties: false
      description: 'Error reply envelope shared by every operation. Runtime-only rule
        (does not export to JSON Schema): code is populated only for DeviceCommandError.
        It may still be None for a DeviceCommandError when the device reports no code.'
      properties:
        status:
          const: error
          default: error
          type: string
        error_type:
          $ref: '#/$defs/ErrorType'
          description: Library exception name to raise
        message:
          description: Human-readable error description
          type: string
        code:
          anyOf:
          - type: integer
          - type: 'null'
          default: null
          description: Device-reported error code, present only for DeviceCommandError
      required:
      - error_type
      - message
      title: Error
      type: object
    ErrorType:
      description: Library exception names reported in an error reply. These are the
        leaf exception types the client maps an error reply back onto. The abstract
        root MightexLEDError is intentionally absent because it is never raised directly.
        DEVICE_CONNECTION and DEVICE_NOT_FOUND are both present even though the latter
        is a subtype of the former, so the client can map directly to the most specific
        class.
      enum:
      - ValueError
      - ControllerClosedError
      - DeviceConnectionError
      - DeviceNotFoundError
      - DeviceCommandError
      - UnsupportedOperationError
      title: ErrorType
      type: string
  discriminator:
    mapping:
      error: '#/$defs/Error'
      ok: '#/$defs/DeviceInfoOk'
    propertyName: status
  oneOf:
  - $ref: '#/$defs/DeviceInfoOk'
  - $ref: '#/$defs/Error'
