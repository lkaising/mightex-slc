# Generated by contract/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in contract/mightex_contract/.
operation: read_load_voltage
request:
  additionalProperties: false
  description: Read the present load voltage on a channel. Load voltage read-back
    is a voltage-monitoring module variant feature; on modules without it the server
    reports UnsupportedOperationError. The reading is meaningful only in NORMAL mode
    or a slow STROBE mode, because the controller polls the load at a 20 ms interval.
  properties:
    device_id:
      description: Handle returned by open_device
      title: Device Id
      type: string
    channel:
      description: One-based channel number
      minimum: 1
      title: Channel
      type: integer
  required:
  - device_id
  - channel
  title: ReadLoadVoltageRequest
  type: object
reply:
  $defs:
    Error:
      additionalProperties: false
      description: 'Error reply envelope shared by every operation. Runtime-only rule
        (does not export to JSON Schema): code is populated only for DeviceCommandError.
        It may still be None for a DeviceCommandError when the device reports no code.'
      properties:
        status:
          const: error
          default: error
          title: Status
          type: string
        error_type:
          $ref: '#/$defs/ErrorType'
          description: Library exception name to raise
        message:
          description: Human-readable error description
          title: Message
          type: string
        code:
          anyOf:
          - type: integer
          - type: 'null'
          default: null
          description: Device-reported error code, present only for DeviceCommandError
          title: Code
      required:
      - error_type
      - message
      title: Error
      type: object
    ErrorType:
      description: Library exception names reported in an error reply. These are the
        leaf exception types the client maps an error reply back onto. The abstract
        root MightexLEDError is intentionally absent because it is never raised directly.
        DEVICE_CONNECTION and DEVICE_NOT_FOUND are both present even though the latter
        is a subtype of the former, so the client can map directly to the most specific
        class.
      enum:
      - ValueError
      - ControllerClosedError
      - DeviceConnectionError
      - DeviceNotFoundError
      - DeviceCommandError
      - UnsupportedOperationError
      title: ErrorType
      type: string
    ReadLoadVoltageOk:
      additionalProperties: false
      description: Successful read-load-voltage reply.
      properties:
        status:
          const: ok
          default: ok
          title: Status
          type: string
        result:
          description: Present load voltage in millivolts
          title: Result
          type: integer
      required:
      - result
      title: ReadLoadVoltageOk
      type: object
  discriminator:
    mapping:
      error: '#/$defs/Error'
      ok: '#/$defs/ReadLoadVoltageOk'
    propertyName: status
  oneOf:
  - $ref: '#/$defs/ReadLoadVoltageOk'
  - $ref: '#/$defs/Error'
