# Generated by contract/generate_schemas.py. Do not edit.
# Source of truth: the Pydantic models in contract/mightex_contract/.
operation: configure_strobe
request:
  $defs:
    ProfileStep:
      additionalProperties: false
      description: 'A single profile step.


        Note: the public API skeleton expresses a step as a bare tuple

        (current_ma, time_us). The contract deliberately replaces that tuple with

        this named model, so the serialized form is

        {"current_ma": 10.0, "time_us": 1000} rather than [10.0, 1000]. That object

        form is the intended public surface.'
      properties:
        current_ma:
          description: Step current in milliamps
          minimum: 0
          title: Current Ma
          type: number
        time_us:
          description: Step duration in whole microseconds
          minimum: 0
          title: Time Us
          type: integer
      required:
      - current_ma
      - time_us
      title: ProfileStep
      type: object
  additionalProperties: false
  description: 'Store STROBE mode parameters for a channel.


    The number of profile steps must not exceed the module''s max_profile_steps.

    That per-module limit is enforced server-side at runtime and does not export

    to JSON Schema; the static Profile bound is only the absolute 127-step

    ceiling.'
  properties:
    device_id:
      description: Handle returned by open_device
      title: Device Id
      type: string
    channel:
      description: One-based channel number
      minimum: 1
      title: Channel
      type: integer
    current_max_ma:
      description: Maximum current in milliamps
      minimum: 0
      title: Current Max Ma
      type: number
    repeat_count:
      default: 0
      description: Device repeat count; the profile is output repeat_count + 1 times,
        or indefinitely when equal to REPEAT_FOREVER (9999). Defaults to 0, which
        outputs the profile once.
      maximum: 99999999
      minimum: 0
      title: Repeat Count
      type: integer
    profile:
      description: Ordered profile steps without the terminating zero pair; an empty
        profile leaves the channel off in STROBE mode
      items:
        $ref: '#/$defs/ProfileStep'
      maxItems: 127
      title: Profile
      type: array
  required:
  - device_id
  - channel
  - current_max_ma
  - profile
  title: ConfigureStrobeRequest
  type: object
reply:
  $defs:
    ConfigureStrobeOk:
      additionalProperties: false
      description: Successful configure-strobe reply.
      properties:
        status:
          const: ok
          default: ok
          title: Status
          type: string
      title: ConfigureStrobeOk
      type: object
    Error:
      additionalProperties: false
      description: 'Error reply envelope shared by every operation.


        Runtime-only rule (does not export to JSON Schema): code is populated only

        for DeviceCommandError. It may still be None for a DeviceCommandError when

        the device reports no code.'
      properties:
        status:
          const: error
          default: error
          title: Status
          type: string
        error_type:
          $ref: '#/$defs/ErrorType'
          description: Library exception name to raise
        message:
          description: Human-readable error description
          title: Message
          type: string
        code:
          anyOf:
          - type: integer
          - type: 'null'
          default: null
          description: Device-reported error code, present only for DeviceCommandError
          title: Code
      required:
      - error_type
      - message
      title: Error
      type: object
    ErrorType:
      description: 'Library exception names reported in an error reply.


        These are the leaf exception types the client maps an error reply back onto.

        The abstract root MightexLEDError is intentionally absent because it is never

        raised directly. DEVICE_CONNECTION and DEVICE_NOT_FOUND are both present even

        though the latter is a subtype of the former, so the client can map directly

        to the most specific class.'
      enum:
      - ValueError
      - ControllerClosedError
      - DeviceConnectionError
      - DeviceNotFoundError
      - DeviceCommandError
      - UnsupportedOperationError
      title: ErrorType
      type: string
  discriminator:
    mapping:
      error: '#/$defs/Error'
      ok: '#/$defs/ConfigureStrobeOk'
    propertyName: status
  oneOf:
  - $ref: '#/$defs/ConfigureStrobeOk'
  - $ref: '#/$defs/Error'
